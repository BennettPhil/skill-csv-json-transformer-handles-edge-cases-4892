# csv-json-transformer-handles-edge-cases-4892

A CSV-to-JSON transformer that handles edge cases gracefully. It should support custom delimiters, quoted f...

## Quick Start

```bash
./scripts/run.sh demo-target
```

## Prerequisites

- Bash 3.2+ (macOS compatible)
